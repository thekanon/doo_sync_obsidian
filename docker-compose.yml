services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: doo-sync-obsidian:latest
    container_name: doo-sync-obsidian
    env_file: [ .env.docker ]
    ports:
      - "33001:33001"
    restart: unless-stopped
    # 운영 실행 시 기본(빌드된 standalone 사용)